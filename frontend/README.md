# Frontend

React + Vite client for InterviewAI.

## Run locally

```powershell
npm install
Copy-Item .env.example .env
npm run dev
```

The app opens at `http://localhost:5173`. Chrome or Edge provides the best Web Speech API support.
